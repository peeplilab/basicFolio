<%include partials/header%>
<header  class="jumbotron" style="background-image: url('<%=project.image%>')">
        <div  class="container text-center">
            <div style="padding:15vh">

            </div>
            
           
           
         </div>
</header>

<div class="container">
        <div class="col-md-12">
        <h1 class="mt-3"><%=project.name%></h1>
            <hr>
        <p id="thext" class="lead center"> 
            <%=project.description%>         
        </p>
    </div>
    


<%if(currentUser) {%>
<a class="btn btn-sm btn-warning" href="/projects/<%=project._id%>/edit">Edit</a>
<form style="display: inline;" action="/projects/<%=project._id%>?_method=DELETE" method="POST">
<button class="btn btn-danger btn-sm" >Delete</button>
</form>
<%}%>
<%include partials/footer%>
